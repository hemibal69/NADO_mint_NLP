# NADO Mint NLP Bot

**Автоматический бот для пополнения пула Nado Liquidity Provider (NLP)**  

Бот отслеживает доступный лимит mint для твоего subaccount и готовит транзакцию, когда появляется свободное место в пуле.

---

## 🔹 Особенности
- Проверка баланса NLP-пула.  
- Проверка текущего лимита `max_nlp_mintable` для subaccount.  
- Автоматическое формирование payload для `mint_lp`.  
- Логирование всех действий в консоль.  
- Возможность интеграции с уведомлениями (Telegram / email) — опционально.  

---

## 🔹 Установка

# Клонируем репозиторий
```bash
git clone https://github.com/hemibal69/NADO_mint_NLP.git
cd NADO_mint_NLP
```
# Создаём виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # Linux / Mac
venv\Scripts\activate     # Windows
```
# Устанавливаем зависимости
```bash
pip install -r requirements.txt
```

## 🔹 Настройка

В коде NLP.py уже используются заглушки для приватного ключа и subaccount:
```bash
PRIVATE_KEY = "0xВАШ_PRIVATE_KEY"
SENDER = "ВАШ_subaccount_number"
```
- Для работы бота замените эти значения на свои реальные данные.
- В будущем можно вынести их в .env для безопасности.

## 🔹 Запуск
```bash
python NLP.py
```
- Бот проверяет пул каждые CHECK_INTERVAL секунд (по умолчанию 60).
- Когда появляется доступный лимит, бот готовит транзакцию на депозиты в пул.

## 🔹 Требования
- Python 3.9+
- Библиотеки:
	•	requests
	•	eth-account
	•	python-dotenv (если используете .env)
